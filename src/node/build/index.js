#!/usr/bin/env -S NODE_OPTIONS='--experimental-specifier-resolution=node' node

// # Database building
// This Node script saves a character-names database to a file called `build/database`,
// relative to the project directory.
